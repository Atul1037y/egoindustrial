# @package _global_
defaults:
  - /train/default
  - _self_

max_epochs: 30

model:
  freeze_backbone: true
  pretrained: true

optimizer:
  lr: 1e-4
  weight_decay: 0.01

scheduler:
  warmup_epochs: 3
  max_epochs: 30

loss:
  verb_weight: 1.0
  noun_weight: 1.0
  action_weight: 2.0
  