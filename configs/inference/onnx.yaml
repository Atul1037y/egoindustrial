# @package _global_
_target_: egoindustrial.inference.export_onnx.export_to_onnx

checkpoint: ${paths.checkpoint_dir}/best.ckpt
output_path: ${paths.output_root}/model.onnx
input_shape: [1, 3, 16, 224, 224]
opset_version: 17
dynamic_axes: true
simplify: true
