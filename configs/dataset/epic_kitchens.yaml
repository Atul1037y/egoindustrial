# @package _global_
defaults:
  - /dataset/transforms: videomae
  - _self_

name: epic_kitchens
root: ${paths.data_root}/epic_kitchens
split: train
clip_len: 16
frame_stride: 2
use_action_labels: true
batch_size: 32
num_workers: 8
domain_weight: 1.0
